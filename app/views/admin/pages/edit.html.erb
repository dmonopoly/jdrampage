<!--admin/pages/edit-->
<div id="page_edit">
	<%= render :partial => "form", :locals => { :page => @page, :button_value => "Update" } %>
  <% if can? :delete, Page %>
    <p><%= link_to "Delete", [:admin, @page], :method => :delete, :confirm => "Are you sure?" %></p>
  <% end %>
</div>

<div id="directions">
	<%= link_to "<< Back", admin_pages_path %> |
	<%= link_to "Show", @page %> (changes will not be saved)
</div>

