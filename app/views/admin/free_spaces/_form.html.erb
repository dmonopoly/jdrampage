<!--admin/free_spaces/form-->
<%= form_for(@free_space) do |f| %>
  <% if @free_space.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@free_space.errors.count, "error") %> prohibited this free_space from being saved:</h2>

      <ul>
      <% @free_space.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <p>
      <%= f.label :name %>
      <%= f.text_field :name %>
    </p>
    <p>
      <%= f.label :content %>
      <%= f.text_area :content %>
    </p>
    <p>
      <%= f.label :is_active, "Active?" %>
      <%= f.text_field :is_active %>
    </p>
    <p>
      <%= f.submit %>
    </p>
  </div>
<% end %>

